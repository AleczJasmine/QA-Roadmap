# January 20, 2025

Learned about substrings and substring comparison

Basically:

String s = sc.next();
s.subtring(start,end); <-- indexes

Also learned about comparison of alphabetically arranged  Strings using the compareTo() method 

Heres is how to use it

public class StringComparison {
    public static void main(String[] args) {
        String str1 = "apple";
        String str2 = "banana";

        // Compare strings
        if (str1.compareTo(str2) > 0) {
            System.out.println(str1 + " is lexicographically greater than " + str2);
        } else if (str1.compareTo(str2) < 0) {
            System.out.println(str1 + " is lexicographically less than " + str2);
        } else {
            System.out.println(str1 + " is lexicographically equal to " + str2);
        }
    }
}

Related learning items are Code 18 and Code 19
